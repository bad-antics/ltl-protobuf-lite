// examples/demo.ltl — runnable demo for ltl_protobuf_lite

import std::io
use ltl_protobuf_lite as lib

fn main() {
    io::println("ltl_protobuf_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}